The task manager allows the users to manage their tasks efficiently using a menu-driven interface. It is designed for students or anyone who wants a lightweight way to track deadlines directlyin the terminal. The task manager can: Add tasks with deadlines, View all tasks with completion status, Mark tasks as completed, Show urgent tasks (due within three days), And it has a simple menu-driven interface. 
